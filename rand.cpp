#include <cstdlib>

using std::endl;
using std::cout;

int main()
{
    unsigned int seed{0};
    cout << "Enter SEED>";
    cin >> seed;

    srand(seed);

    cout << rand
    cout << rand
    cout << rand
}